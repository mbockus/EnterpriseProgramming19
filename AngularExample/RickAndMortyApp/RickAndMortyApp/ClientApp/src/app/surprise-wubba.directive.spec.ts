import { SurpriseWubbaDirective } from './surprise-wubba.directive';

describe('SurpriseWubbaDirective', () => {
  it('should create an instance', () => {
    const directive = new SurpriseWubbaDirective();
    expect(directive).toBeTruthy();
  });
});
